#ifndef LCS_DISTANCE_HPP
#define LCS_DISTANCE_HPP

#include <string>

std::size_t lcs_length(const std::string & w1, const std::string & w2);
std::size_t lcs_distance(const std::string & w1, const std::string & w2);

#endif
